# Server
PORT=5000
NODE_ENV=development

# MySQL (configure via MySQL Workbench)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=beverage_vault

# Auth
JWT_SECRET=change-me-before-production

# Frontend origin (CORS)
CORS_ORIGIN=http://localhost:3000
